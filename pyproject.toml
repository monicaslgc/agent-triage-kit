[project]
name = "agentops-triage-kit"
version = "0.1.0"
description = "A small, dependency-free multi-agent triage/escalation pipeline demo."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
